os: linux
dist: focal
language: cpp

install:
 - echo install_step
# - sudo apt-get update
# - sudo apt-get install --yes valgrind
# - sudo apt-get install --yes libgtest-dev cmake
# - sudo apt-get install --yes cmake
# - sudo apt-get install --yes cppcheck
# - sudo apt-get install --yes gcovr
jobs:
 - stage: test
   script: echo 1
 - script: echo 2
